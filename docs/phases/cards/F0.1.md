# TASK F0.1 — Dependency & tooling configuration

> **Assigned model: DeepSeek.** Rationale: this is exact-syntax boilerplate (TOML tables, tool
> config keys, package extras) where correctness of key names matters more than creativity.
> DeepSeek is the most literal of the three about following a fixed output format and does not
> pad config files with invented options. Gemini tends to add unrequested keys; Grok tends to
> add prose around the code.

---

## PROMPT — copy everything below this line

You are writing two files for the backend of an "Online Cinema" project (a learning project held to production discipline). You cannot see the repository — everything you need is below. Output **file contents only**, no explanations.

### Project stack

Python 3.12 · FastAPI · SQLAlchemy 2.0 (async, `Mapped`/`mapped_column`) · asyncpg · Alembic · PostgreSQL 16 · Pydantic v2 · Redis · Celery + celery-beat · MinIO (S3) · Stripe · Poetry · Docker Compose · pytest + pytest-asyncio · mypy strict · ruff + black.

Code layout: package `src/`, tests in `tests/` with subpackages `unit/`, `integration/`, `e2e/`.

---

### FILE 1 — `pyproject.toml`

Poetry. Python `^3.12`. The package is `src`.

**Group `main`:** fastapi, uvicorn[standard], sqlalchemy[asyncio], asyncpg, alembic, pydantic, pydantic-settings, pyjwt, passlib[bcrypt], python-multipart, celery, redis, jinja2, aiosmtplib, boto3, stripe, pillow.

**Group `dev`:** pytest, pytest-asyncio, pytest-cov, httpx, aiosqlite, ruff, black, mypy, faker, polyfactory, types-passlib.

Tool configuration lives in the same file:

- `[tool.ruff]` — line-length 88, select `E,F,I,B,UP,SIM,C4,ANN`, target-version py312
- `[tool.black]` — line-length 88
- `[tool.mypy]` — `strict = true`, `plugins = ["pydantic.mypy"]`, `ignore_missing_imports` for `stripe` and `passlib`
- `[tool.pytest.ini_options]` — `asyncio_mode = "auto"`, `testpaths = ["tests"]`, markers `unit`, `integration`, `e2e`
- `[tool.coverage.run]` — `source = ["src"]`, omit Alembic migration files

Add nothing beyond what is listed above: no extra dependencies, no extra tool sections, no extra config keys.

---

### FILE 2 — `.env.sample`

Every variable gets a placeholder value and a short English comment above it describing what it is. Group variables into logical blocks with header comments. This is the exact and complete set of variables — add nothing, omit nothing:

```
POSTGRES_USER, POSTGRES_PASSWORD, POSTGRES_DB, POSTGRES_HOST, POSTGRES_PORT
SECRET_KEY_ACCESS, SECRET_KEY_REFRESH, JWT_ALGORITHM
ACCESS_TOKEN_TTL_MINUTES, REFRESH_TOKEN_TTL_DAYS
ACTIVATION_TOKEN_TTL_HOURS, PASSWORD_RESET_TTL_MINUTES
EMAIL_HOST, EMAIL_PORT, EMAIL_USER, EMAIL_PASSWORD, EMAIL_FROM, EMAIL_USE_TLS
REDIS_URL, CELERY_BROKER_URL, CELERY_RESULT_BACKEND
S3_ENDPOINT, S3_ACCESS_KEY, S3_SECRET_KEY, S3_BUCKET_NAME
STRIPE_SECRET_KEY, STRIPE_WEBHOOK_SECRET, STRIPE_SUCCESS_URL, STRIPE_CANCEL_URL
ENVIRONMENT, DOCS_ENABLED
```

Line format: `KEY=placeholder`. Never write real secrets — use obvious placeholders (`changeme`, `your-key-here`, and similar).

Important: a later automated test compares the set of keys in `.env.sample` against the set of fields on the `Settings` class. The key set must therefore be exactly the list above — no extras, no omissions.

---

### PROHIBITIONS

```
FORBIDDEN:
- Synchronous SQLAlchemy (Session, create_engine, query()). Async only: AsyncSession,
  create_async_engine, select(), Mapped[], mapped_column().
- The legacy declarative form Column(...) without a Mapped[] annotation.
- Pydantic v1 (class Config, @validator, orm_mode). v2 only:
  model_config = ConfigDict(...), @field_validator, from_attributes=True.
- raise HTTPException anywhere. Only exceptions from core/exceptions.py.
- os.getenv / os.environ outside core/config.py.
- Importing fastapi inside services/, repositories/, models/.
- float for money. Decimal only.
- datetime.utcnow(). Only datetime.now(timezone.utc).
- Creating files not named in the task.
- Stubs: TODO / pass / "implementation goes here". The code must be complete.
- Explanatory text outside the code. Return file contents only.

REQUIRED:
- Full type annotations including return types (must pass mypy --strict).
- A docstring on every public class and function, in English.
- If you need a module that does not exist yet, do not invent its contents — import it at
  the path and signature given in the task.
```

---

### OUTPUT FORMAT

Exactly two blocks, in this order, with no text between or after them:

````
### pyproject.toml
```toml
<contents>
```

### .env.sample
```
<contents>
```
````
