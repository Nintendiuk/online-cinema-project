# TASK F2.5 — Celery application and beat schedule

You are writing two files for a production-discipline FastAPI project (Online Cinema).
You cannot see the repository, so every external symbol you need is given below.

## FORBIDDEN / REQUIRED

```
FORBIDDEN:
- Synchronous SQLAlchemy. Async only: AsyncSession, select(), Mapped[], mapped_column().
- Pydantic v1 idioms (class Config, @validator, orm_mode). v2 only:
  model_config = ConfigDict(from_attributes=True), @field_validator.
- raise HTTPException anywhere. Only exceptions from core/exceptions.py.
- os.getenv / os.environ outside core/config.py.
- Importing fastapi inside services/, repositories/, models/, integrations/.
- Business logic in schemas. Schemas validate shape, not domain rules.
- Any home-grown password strength check. Call
  src.security.validators.validate_password_strength instead.
- Importing or reintroducing passlib. Hashing already exists in
  src.security.passwords and must not be reimplemented.
- Concrete transport code (smtplib, aiosmtplib) inside an interface file.
- datetime.utcnow(). Use datetime.now(timezone.utc).
- str + Enum double inheritance. Use enum.StrEnum.
- Creating files not listed in the task.
- Placeholders: TODO, pass, "implementation goes here". Code must be complete.
- Prose outside the code. Return file contents only.

REQUIRED:
- Full type annotations including return types (must pass mypy --strict).
- A docstring on every public class and function, in English.
- If you need a module that does not exist yet, import it at the path and signature
  given in the task. Do not invent its contents.
```

## Environment

Python 3.12 · Celery 5.4 · mypy --strict · ruff (rules E,F,I,B,UP,SIM,C4,ANN) ·
black, line length 88.

## Existing symbols — import them, do not redefine them

From `src/core/config.py`:

```python
def get_settings() -> Settings:
    """Return the process-wide settings instance, building it on first call."""
```

The two attributes you need on the returned object are named **exactly**:

```python
settings.celery_broker_url      # str
settings.celery_result_backend  # str
```

They are lower-case snake_case attributes on a Pydantic-settings object. Writing
`settings.CELERY_BROKER_URL` raises `AttributeError`. Never call `os.getenv` — the
settings object is the only permitted source of configuration.

## Files to produce

### 1. `src/tasks/__init__.py`

A module docstring only, describing the package as the home of Celery tasks and the
Celery application. No imports.

### 2. `src/tasks/celery_app.py`

A `Celery` application instance named `celery_app`, whose Celery app name is
`"online_cinema"`, with the broker and the result backend taken from `get_settings()`.

Configuration, applied via `celery_app.conf.update(...)`:

| Key | Value |
|---|---|
| `task_serializer` | `"json"` |
| `accept_content` | `["json"]` |
| `result_serializer` | `"json"` |
| `timezone` | `"UTC"` |
| `enable_utc` | `True` |
| `task_track_started` | `True` |
| `task_time_limit` | `300` |

Then: `celery_app.autodiscover_tasks(["src.tasks"])`.

Beat schedule — exactly one entry, hourly on the minute:

```python
celery_app.conf.beat_schedule = {
    "purge-expired-activation-tokens": {
        "task": "src.tasks.tokens.purge_expired_activation_tokens",
        "schedule": crontab(minute=0),
    },
}
```

`crontab` is imported from `celery.schedules`.

### Constraints that decide whether this file is accepted

1. **`src/tasks/tokens.py` is written by another party. Do not create it, do not import
   it.** The beat entry references the task by its dotted string name only — that string
   is resolved lazily by the worker at dispatch time, so the module does not need to
   exist for this file to import.

2. **The module must import cleanly with no Redis running.** Constructing a `Celery`
   object and updating its configuration performs no I/O; the connection is opened when a
   worker starts or a task is dispatched. Therefore: no `celery_app.connection()`, no
   `ping`, no broker check, no `autoretry` connection probe, and nothing at import time
   that touches the network. A unit test imports this module in an environment with no
   broker at all.

3. Read the settings **once** at module level (`settings = get_settings()`), the way the
   rest of the project does, rather than calling `get_settings()` repeatedly inline.

4. Give the module a docstring explaining that the worker and beat containers load this
   module as `src.tasks.celery_app:celery_app`, and that task modules register themselves
   through `autodiscover_tasks`.

Note for context, requiring no action from you: an older Celery module elsewhere in the
project is being deleted as part of this phase, and the container commands are being
repointed at this file. Yours is the single Celery application. Do not reference the old
module or try to import from it.

## Output

Return the contents of the two files and nothing else. Prefix each with a comment line
giving its path. No explanation, no summary, no prose outside the code blocks.
